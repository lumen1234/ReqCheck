# 装备软件智能测试一体化解决方案 - 前端工程规范

> 基于"单元测试"模块提取的可复用工程规范  
> 适用于：文档审查、需求理解、代码分析、单元测试、集成测试、代码修复等所有前端模块

**版本**: v1.0  
**更新日期**: 2026年2月9日

---

## 📚 目录

- [1. 技术栈](#1-技术栈)
- [2. 项目结构](#2-项目结构)
- [3. 设计系统](#3-设计系统)
- [4. 开发规范](#4-开发规范)
- [5. 状态管理](#5-状态管理)
- [6. 路由配置](#6-路由配置)
- [7. API集成](#7-api集成)
- [8. 组件规范](#8-组件规范)
- [9. 构建与部署](#9-构建与部署)

---

## 1. 技术栈

### 1.1 核心框架
- **Vue 3.3.11+** - 采用 Composition API + `<script setup>` 语法
- **Vite 5.0.8+** - 现代化构建工具
- **TypeScript** (可选) - 根据团队需求

### 1.2 路由与状态管理
- **Vue Router 4.2.5+** - 官方路由解决方案
- **Pinia 2.1.7+** - 官方状态管理库（替代Vuex）

### 1.3 UI 与样式
- **Tailwind CSS 3.3.6+** - 原子化CSS框架
- **PostCSS + Autoprefixer** - CSS后处理
- **Lucide Vue Next 0.294.0+** - 图标库（轻量级，支持tree-shaking）

### 1.4 工具库
- **Axios 1.6.2+** - HTTP客户端
- **Highlight.js 11.9.0+** - 代码高亮（如需展示代码）

### 1.5 开发依赖
```json
{
  "@vitejs/plugin-vue": "^4.5.2",
  "autoprefixer": "^10.4.16",
  "postcss": "^8.4.32",
  "tailwindcss": "^3.3.6",
  "vite": "^5.0.8"
}
```

---

## 2. 项目结构

### 2.1 标准目录结构

```
project-root/
├── public/                 # 静态资源
├── src/
│   ├── components/        # 可复用组件
│   │   ├── NavBar.vue    # 导航栏组件（必需）
│   │   └── ...           # 其他共享组件
│   ├── views/            # 页面级组件
│   │   ├── View1.vue
│   │   ├── View2.vue
│   │   └── ...
│   ├── router/
│   │   └── index.js      # 路由配置
│   ├── store/
│   │   └── index.js      # Pinia Store
│   ├── api/              # API接口封装（推荐）
│   │   └── index.js
│   ├── utils/            # 工具函数（推荐）
│   ├── App.vue           # 根组件
│   ├── main.js           # 应用入口
│   └── style.css         # 全局样式
├── index.html
├── package.json
├── vite.config.js
├── tailwind.config.js
├── postcss.config.js
└── README.md
```

### 2.2 文件命名规范

- **组件**: PascalCase - `NavBar.vue`, `FileTree.vue`
- **视图**: PascalCase + `View` 后缀 - `UploadView.vue`, `BrowseView.vue`
- **工具/API**: camelCase - `apiClient.js`, `formatDate.js`
- **配置文件**: kebab-case - `vite.config.js`, `tailwind.config.js`

---

## 3. 设计系统

### 3.1 色彩规范

#### 主色调 (Primary - 蓝色系)
```javascript
primary: {
  50: '#eff6ff',
  100: '#dbeafe',
  200: '#bfdbfe',
  300: '#93c5fd',
  400: '#60a5fa',
  500: '#3b82f6',
  600: '#2563eb',
  700: '#1d4ed8',
  800: '#1e40af',
  900: '#1e3a8a',  // 主要使用
  950: '#172554',
}
```

#### 中性色 (Slate 系列 - Tailwind 内置)
- **文本**: `text-slate-900` (主要), `text-slate-700` (次要), `text-slate-500` (辅助)
- **背景**: `bg-white` (主背景), `bg-slate-50` (次级背景), `bg-slate-100` (卡片hover)
- **边框**: `border-slate-200` (主要), `border-slate-300` (强调)

#### 功能色
- **成功**: `text-green-600`, `bg-green-50`, `bg-green-500`
- **警告**: `text-yellow-600`, `bg-yellow-50`
- **错误**: `text-red-600`, `bg-red-50`, `hover:text-red-500`
- **信息**: 使用 Primary 色系

### 3.2 字体规范

#### 字重
- **Regular** (400): 不常用，主要用于长文本
- **Medium** (500): `font-medium` - 辅助文本、说明文字
- **Semibold** (600): `font-semibold` - 按钮文字、强调文本
- **Bold** (700): `font-bold` - 标题、导航、重要标签

#### 字号
```css
text-xs     /* 11px - 小标签 */
text-sm     /* 14px - 按钮、表单 */
text-base   /* 16px - 正文 */
text-lg     /* 18px - 副标题 */
text-xl     /* 20px - 页面小标题 */
text-2xl    /* 24px - 卡片标题 */
text-3xl    /* 30px - 页面主标题 */
```

### 3.3 间距规范

#### Padding/Margin
- **组件内边距**: `p-2` (8px), `p-4` (16px), `p-6` (24px), `p-8` (32px)
- **元素间距**: `space-x-2/space-y-2` (8px), `space-x-4/space-y-4` (16px)
- **栅格间距**: `gap-2` (8px), `gap-4` (16px), `gap-6` (24px)

#### 布局容器
```html
<!-- 页面容器 -->
<div class="max-w-7xl mx-auto">

<!-- 卡片容器 -->
<div class="max-w-4xl mx-auto">
```

### 3.4 圆角规范

```css
rounded      /* 4px - 小元素 */
rounded-lg   /* 8px - 卡片、按钮 */
rounded-xl   /* 12px - 大卡片 */
rounded-full /* 完全圆角 - 标签、头像 */
```

### 3.5 阴影规范

```css
shadow-sm    /* 轻微阴影 - 卡片 */
shadow-md    /* 中等阴影 - 按钮 */
shadow-lg    /* 明显阴影 - 悬浮卡片 */
shadow-2xl   /* 强阴影 - 浮动按钮 */
```

### 3.6 动画与过渡

```css
transition-all duration-200  /* 通用过渡 */
transition-colors            /* 颜色过渡 */
transition-transform         /* 形变过渡 */

hover:scale-105             /* 悬浮放大 */
active:scale-95             /* 点击缩小 */
```

---

## 4. 开发规范

### 4.1 Vue 3 编码规范

#### 推荐：Composition API + `<script setup>`

```vue
<template>
  <div class="container">
    <h1>{{ title }}</h1>
    <button @click="handleClick">{{ buttonText }}</button>
  </div>
</template>

<script setup>
import { ref, computed, onMounted } from 'vue'
import { useRouter } from 'vue-router'
import { useAppStore } from '@/store'

// Props
const props = defineProps({
  initialTitle: String
})

// Emits
const emit = defineEmits(['update'])

// Composables
const router = useRouter()
const store = useAppStore()

// State
const title = ref(props.initialTitle)
const count = ref(0)

// Computed
const buttonText = computed(() => `点击次数: ${count.value}`)

// Methods
const handleClick = () => {
  count.value++
  emit('update', count.value)
}

// Lifecycle
onMounted(() => {
  console.log('Component mounted')
})
</script>

<style scoped>
/* 组件特定样式 - 尽量使用 Tailwind */
</style>
```

#### 组件定义顺序
1. Import 语句
2. Props 定义
3. Emits 定义
4. Composables (router, store等)
5. State (ref, reactive)
6. Computed
7. Methods
8. Lifecycle hooks

### 4.2 命名规范

```javascript
// 变量/函数: camelCase
const isLoading = ref(false)
const fetchData = async () => {}

// 组件: PascalCase
import NavBar from '@/components/NavBar.vue'

// 常量: UPPER_SNAKE_CASE
const API_BASE_URL = 'http://localhost:8000'

// Store: use + PascalCase + Store
const useAppStore = defineStore('app', {})
```

### 4.3 注释规范

```vue
<script setup>
// ==================== 组件配置 ====================
import { ref } from 'vue'

// ==================== Props & Emits ====================
const props = defineProps({
  // 文件ID
  fileId: {
    type: String,
    required: true
  }
})

// ==================== State ====================
const isLoading = ref(false)  // 加载状态

// ==================== API调用 ====================
/**
 * 获取文件列表
 * @param {string} projectId - 项目ID
 * @returns {Promise<Array>} 文件列表
 */
const fetchFiles = async (projectId) => {
  // 实现...
}
</script>
```

---

## 5. 状态管理

### 5.1 Pinia Store 结构

```javascript
// src/store/index.js
import { defineStore } from 'pinia'

export const useAppStore = defineStore('app', {
  // ==================== State ====================
  state: () => ({
    // 持久化字段（使用 localStorage）
    projectId: localStorage.getItem('projectId') || null,
    projectName: localStorage.getItem('projectName') || null,
    
    // 临时字段（页面级，不持久化）
    currentStep: 0,
    isLoading: false,
  }),
  
  // ==================== Getters ====================
  getters: {
    hasProject: (state) => state.projectId !== null,
    projectInfo: (state) => ({
      id: state.projectId,
      name: state.projectName
    })
  },
  
  // ==================== Actions ====================
  actions: {
    // 设置项目信息（持久化）
    setProject(id, name) {
      this.projectId = id
      this.projectName = name
      localStorage.setItem('projectId', id)
      localStorage.setItem('projectName', name)
    },
    
    // 清除项目信息
    clearProject() {
      this.projectId = null
      this.projectName = null
      localStorage.removeItem('projectId')
      localStorage.removeItem('projectName')
    },
    
    // 设置临时状态
    setLoading(status) {
      this.isLoading = status
    }
  }
})
```

### 5.2 在组件中使用

```vue
<script setup>
import { useAppStore } from '@/store'

const store = useAppStore()

// 读取状态
console.log(store.projectId)
console.log(store.hasProject)

// 调用 actions
store.setProject('123', 'My Project')
</script>
```

### 5.3 LocalStorage 使用规范

- **持久化数据**: 用户选择的项目、配置等需要跨会话保留的数据
- **格式**: 统一使用字符串形式存储
- **清理**: 在相关数据变更时及时清理依赖的localStorage项

---

## 6. 路由配置

### 6.1 路由结构

```javascript
// src/router/index.js
import { createRouter, createWebHistory } from 'vue-router'
import { useAppStore } from '@/store'

const routes = [
  {
    path: '/',
    name: 'step1',
    component: () => import('@/views/Step1View.vue'),
    meta: { title: '第一步', requiresAuth: false }
  },
  {
    path: '/step2',
    name: 'step2',
    component: () => import('@/views/Step2View.vue'),
    meta: { title: '第二步', requiresAuth: true },
    beforeEnter: (to, from, next) => {
      const store = useAppStore()
      if (!store.projectId) {
        next('/')  // 重定向到起始页
      } else {
        next()
      }
    }
  },
  // 更多路由...
]

const router = createRouter({
  history: createWebHistory(),
  routes,
})

// 全局路由守卫（可选）
router.beforeEach((to, from, next) => {
  document.title = to.meta.title || '默认标题'
  next()
})

export default router
```

### 6.2 路由守卫使用场景

- **beforeEnter**: 单个路由的访问控制（验证上一步是否完成）
- **beforeEach**: 全局守卫（设置页面标题、权限验证等）
- **afterEach**: 全局后置钩子（分析统计等）

---

## 7. API集成

### 7.1 Axios 配置

```javascript
// src/api/index.js
import axios from 'axios'

// 创建 axios 实例
const apiClient = axios.create({
  baseURL: '/api',  // Vite 代理会将 /api 转发到后端
  timeout: 30000,
  headers: {
    'Content-Type': 'application/json'
  }
})

// 请求拦截器
apiClient.interceptors.request.use(
  config => {
    // 可以在这里添加 token
    // const token = localStorage.getItem('token')
    // if (token) config.headers.Authorization = `Bearer ${token}`
    return config
  },
  error => Promise.reject(error)
)

// 响应拦截器
apiClient.interceptors.response.use(
  response => response.data,
  error => {
    console.error('API Error:', error.response?.data || error.message)
    return Promise.reject(error)
  }
)

// ==================== API方法封装 ====================
export const uploadProject = (formData) => {
  return apiClient.post('/projects/upload', formData, {
    headers: { 'Content-Type': 'multipart/form-data' }
  })
}

export const getProjects = () => {
  return apiClient.get('/projects')
}

export const getProjectDetail = (id) => {
  return apiClient.get(`/projects/${id}`)
}

export default apiClient
```

### 7.2 Vite 代理配置

```javascript
// vite.config.js
export default defineConfig({
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:8000',  // 后端地址
        changeOrigin: true,
        // rewrite: (path) => path.replace(/^\/api/, '')  // 如需重写路径
      }
    }
  }
})
```

### 7.3 在组件中使用

```vue
<script setup>
import { ref } from 'vue'
import { uploadProject, getProjects } from '@/api'

const projects = ref([])
const loading = ref(false)

const fetchProjects = async () => {
  loading.value = true
  try {
    projects.value = await getProjects()
  } catch (error) {
    console.error('Failed to fetch projects:', error)
  } finally {
    loading.value = false
  }
}
</script>
```

---

## 8. 组件规范

### 8.1 NavBar 组件规范

**功能要求**:
- 显示应用Logo和名称
- 步骤导航（支持多步骤流程）
- 步骤状态指示（未开始、进行中、已完成）
- 当前项目信息展示
- 步骤间的访问控制

**标准实现**: 参考 `src/components/NavBar.vue`

**关键特性**:
```vue
<template>
  <nav class="bg-white border-b border-slate-200 px-6 py-4">
    <!-- Logo区 -->
    <div class="flex items-center space-x-2">
      <div class="w-8 h-8 bg-primary-900 rounded flex items-center justify-center">
        <span class="font-bold text-white">首字母</span>
      </div>
      <span class="text-lg font-bold tracking-tight text-slate-900">模块名称</span>
    </div>
    
    <!-- 步骤导航 -->
    <!-- 使用 router-link + 状态判断 -->
    
    <!-- 项目信息 -->
    <div v-if="store.projectName" class="...">
      <span>Project: {{ store.projectName }}</span>
    </div>
  </nav>
</template>
```

### 8.2 文件上传组件模式

```vue
<template>
  <!-- 拖拽区域 -->
  <div 
    @click="triggerFileInput"
    @dragover.prevent
    @drop.prevent="handleDrop"
    class="border-2 border-dashed border-slate-300 rounded-lg p-12 cursor-pointer hover:border-primary-600"
  >
    <input 
      type="file" 
      ref="fileInput" 
      class="hidden" 
      @change="handleFileChange" 
      accept=".pdf,.docx,.zip"
    />
    <div class="text-center">
      <Upload class="w-8 h-8 mx-auto mb-4" />
      <p class="font-bold">点击或拖拽文件至此处上传</p>
    </div>
  </div>
  
  <!-- 进度条 -->
  <div v-if="uploading" class="mt-4">
    <div class="w-full bg-slate-200 rounded-full h-2">
      <div 
        class="bg-primary-600 h-2 rounded-full transition-all" 
        :style="{ width: uploadProgress + '%' }"
      ></div>
    </div>
  </div>
</template>

<script setup>
import { ref } from 'vue'

const fileInput = ref(null)
const uploading = ref(false)
const uploadProgress = ref(0)

const triggerFileInput = () => {
  fileInput.value.click()
}

const handleFileChange = (e) => {
  const file = e.target.files[0]
  if (file) uploadFile(file)
}

const handleDrop = (e) => {
  const file = e.dataTransfer.files[0]
  if (file) uploadFile(file)
}

const uploadFile = async (file) => {
  // 实现上传逻辑
}
</script>
```

### 8.3 加载状态组件

```vue
<!-- 全屏加载 -->
<div v-if="loading" class="absolute inset-0 flex items-center justify-center bg-white/80 backdrop-blur-[2px] z-50">
  <div class="animate-spin rounded-full h-8 w-8 border-b-2 border-primary-600"></div>
</div>

<!-- 行内加载 -->
<div v-if="loading" class="text-center py-12">
  <div class="animate-spin rounded-full h-8 w-8 border-b-2 border-primary-600 mx-auto"></div>
</div>
```

### 8.4 空状态组件

```vue
<div v-if="items.length === 0" class="bg-slate-50 border border-slate-200 rounded-lg p-12 text-center">
  <Icon class="w-16 h-16 text-slate-300 mx-auto mb-4" />
  <p class="text-slate-400 font-medium">暂无数据</p>
</div>
```

---

## 9. 构建与部署

### 9.1 开发命令

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev
# 访问: http://localhost:5173

# 构建生产版本
npm run build

# 预览生产构建
npm run preview
```

### 9.2 Vite 配置关键点

```javascript
// vite.config.js
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import path from 'path'

export default defineConfig({
  plugins: [vue()],
  
  // 路径别名
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  
  // 开发服务器配置
  server: {
    host: '0.0.0.0',    // 允许外部访问
    port: 5173,
    strictPort: true,   // 端口被占用时报错而非自动切换
    hmr: {
      clientPort: 5173  // HMR端口
    },
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true,
      }
    }
  },
  
  // 构建配置
  build: {
    outDir: 'dist',
    sourcemap: false,
    rollupOptions: {
      output: {
        manualChunks: {
          'vendor': ['vue', 'vue-router', 'pinia'],
          'icons': ['lucide-vue-next']
        }
      }
    }
  }
})
```

### 9.3 环境变量

```bash
# .env.development
VITE_API_BASE_URL=http://localhost:8000

# .env.production
VITE_API_BASE_URL=https://api.production.com
```

使用：
```javascript
const apiUrl = import.meta.env.VITE_API_BASE_URL
```

---

## 10. 代码质量

### 10.1 推荐的开发工具链（可选）

```json
{
  "devDependencies": {
    "eslint": "^8.55.0",
    "eslint-plugin-vue": "^9.19.2",
    "prettier": "^3.1.1",
    "prettier-plugin-tailwindcss": "^0.5.9"
  }
}
```

### 10.2 Git提交规范（建议）

```
feat: 新功能
fix: 修复bug
docs: 文档更新
style: 代码格式调整
refactor: 重构
perf: 性能优化
test: 测试相关
chore: 构建/工具配置
```

---

## 11. 模块适配指南

### 11.1 从"单元测试"到"文档审查"的适配

**需要修改的内容**:

1. **NavBar.vue**: 更新 Logo 首字母、模块名称、步骤定义
   ```javascript
   const steps = [
     { name: '文档上传', path: '/', icon: Upload },
     { name: '文档解析', path: '/parse', icon: FileText },
     { name: '审查分析', path: '/review', icon: CheckCircle },
     { name: '审查报告', path: '/report', icon: FileCheck },
   ]
   ```

2. **package.json**: 更新项目名称
   ```json
   {
     "name": "c-doc-review-frontend"
   }
   ```

3. **index.html**: 更新页面标题
   ```html
   <title>C Document Review System</title>
   ```

4. **views**: 根据文档审查流程创建对应的视图组件

5. **store**: 调整状态字段以匹配文档审查流程
   ```javascript
   state: () => ({
     documentId: null,
     documentName: null,
     reviewId: null,
     // ...
   })
   ```

### 11.2 保持不变的部分

- ✅ 整体项目结构
- ✅ 技术栈配置（Vite, Tailwind, Vue Router, Pinia）
- ✅ 设计系统（颜色、字体、间距）
- ✅ 组件样式风格
- ✅ API集成模式
- ✅ 路由守卫逻辑

---

## 12. 附录

### 12.1 常用 Lucide 图标

```javascript
import {
  Upload,          // 上传
  Download,        // 下载
  File,            // 文件
  FileText,        // 文本文件
  FileCode,        // 代码文件
  Folder,          // 文件夹
  FolderTree,      // 目录树
  Check,           // 勾选
  CheckCircle,     // 勾选圆圈
  X,               // 关闭
  XCircle,         // 错误圆圈
  AlertCircle,     // 警告圆圈
  Info,            // 信息
  ChevronRight,    // 右箭头
  ChevronDown,     // 下箭头
  ChevronLeft,     // 左箭头
  RotateCw,        // 刷新
  Zap,             // 闪电
  BarChart3,       // 柱状图
  Settings,        // 设置
  Search,          // 搜索
} from 'lucide-vue-next'
```

### 12.2 Tailwind 常用类名速查

```css
/* 布局 */
flex, grid, block, inline-block
flex-col, flex-row
items-center, items-start, items-end
justify-between, justify-center, justify-end
gap-{2,4,6,8}
space-x-{2,4,6}, space-y-{2,4,6}

/* 尺寸 */
w-full, h-full
w-{数字}, h-{数字}  /* 4的倍数 */
max-w-{xl,2xl,4xl,7xl}

/* 边距 */
p-{2,4,6,8}, px-{}, py-{}
m-{2,4,6,8}, mx-{}, my-{}

/* 文本 */
text-{xs,sm,base,lg,xl,2xl,3xl}
font-{medium,semibold,bold}
text-{slate-500,slate-700,slate-900}
text-center, text-left, text-right

/* 背景 */
bg-white, bg-slate-{50,100}
bg-primary-{500,600,900}

/* 边框 */
border, border-{2,4}
border-slate-{200,300}
rounded, rounded-{lg,xl,full}

/* 阴影 */
shadow, shadow-{sm,md,lg,2xl}

/* 状态 */
hover:bg-{color}
active:scale-95
disabled:bg-slate-300
transition-all, transition-colors

/* 定位 */
absolute, relative, fixed
top-0, right-0, bottom-0, left-0
z-{10,20,50}
```

---

## 总结

本工程规范基于实际项目提取，涵盖了前端开发的核心要素。遵循本规范可以确保：

1. ✅ **统一的技术栈** - 便于团队协作和代码维护
2. ✅ **一致的设计语言** - 提供良好的用户体验
3. ✅ **规范的代码结构** - 提高代码可读性和可维护性
4. ✅ **高效的开发流程** - 减少决策时间，聚焦业务逻辑
5. ✅ **模块间的可复用性** - 快速启动新模块开发

**下一步**: 参考提供的项目脚手架模板，快速启动"文档审查"模块开发。

---

*文档版本: v1.0 | 维护者: 前端开发团队 | 最后更新: 2026-02-09*
